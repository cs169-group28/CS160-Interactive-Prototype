<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
    <%= f.label :name %><div style="color:#ff0000; display:inline">*</div><br />
    <%= f.text_field :name %>
  </div>

  <div style="color:#ff0000">*required</div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
