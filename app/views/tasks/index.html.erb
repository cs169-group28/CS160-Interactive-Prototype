<div id="top_bar">
  <div id="back_to_events">
    <- Back To Events
  </div>
</div>

<div id="container">
  <div id="event_name">
    <h1>Listing tasks for <%= @event.name %> </h1>
  </div>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tasks.each do |task| %>
  <tr>
    <td><%= link_to 'Show', event_task_path(task.event_id, task) %></td>
    <td><%= link_to 'Edit', edit_event_task_path(task.event_id, task) %></td>
    <td><%= link_to 'Destroy', event_task_path(task.event_id, task), method: :delete, data: { confirm: 'Are you sure?' } %></td>
    <td><%= task.name %></td>
  </tr>
<% end %>
</table>

<br />

<div id="overlay" class="web_dialog_overlay"></div>
  
  <div id="dialog" class="web_dialog">
    <div></div>
  </div>

<%= link_to 'New Task', new_event_task_path, :remote => true %>

</div>